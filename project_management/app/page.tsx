export const dynamic = "force-dynamic"; // Completely legal here on the server level!

import UnifiedProjectTracker from "./dashboard";

export default function Home() {
  return <UnifiedProjectTracker />;
}